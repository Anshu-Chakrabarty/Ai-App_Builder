import Placeholder from "@/components/appbuilder/Placeholder";

export default function Page() {
  return (
    <Placeholder
      title="Documentation"
      body="AppBuilder AI guides you from idea → features → stack → deploy → template → repo → CI/CD → preview → publish."
    />
  );
}
