import Placeholder from "@/components/appbuilder/Placeholder";

export default function Page() {
  return (
    <Placeholder
      title="Notifications"
      body="In-app notifications for preview ready, build started, and go-live events (as shown in Review & Pricing)."
    />
  );
}
